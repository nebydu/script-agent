# Go 빌드 산출물
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go 테스트 산출물
*.test
*.out

# Go 모듈 vendor 디렉토리
vendor/

# Agent 런타임 식별자 — 머신/인스턴스별 영구 식별자이므로 커밋 금지
# (spec v0.2.1 §3.1)
.agent_id

# LOG_JOB file_state 영속 디렉토리 — Agent local only (spec §5.2.3 노트)
.agent_state/

# IDE
.vscode/
.idea/

# Claude Code 자동화 — 자동화 정의(CLAUDE.md, settings.json, hooks, agents)는 추적하고
# codex-gate Stop hook 런타임 상태 파일만 추적 제외 (hub .claude/ 정책과 동일)
.claude/.codex-gate-state
.claude/codex-gate.log
.claude/codex-gate-escalation.log
.claude/.codex-gate-issues.txt
.claude/.codex-gate-stderr.txt
.claude/.codex-last-message.json

# OS
.DS_Store
Thumbs.db
